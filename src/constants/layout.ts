/** Bottom padding for pages with a fixed action bar (no mobile nav). */
export const FIXED_BOTTOM_ACTION_CONTENT_CLASS = 'pb-28';

/** Tailwind classes for fixed bottom action bars aligned with the app shell. */
export const FIXED_BOTTOM_ACTION_BAR_CLASS =
  'fixed bottom-0 left-0 right-0 z-40 mx-auto w-full max-w-md border-x border-t border-line bg-cream-50 px-4 py-3 pb-[calc(0.75rem+env(safe-area-inset-bottom))]';
